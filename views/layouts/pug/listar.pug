//- extends layout/main 

block content

    style.
        .table td, .table th {
            vertical-align: middle;
        }
        h1 {
            color: blue;
        }
        hr {
            background-color: #ddd;
        }
        .jumbotron {
            min-height: 100vh;
        }

    div(class="jumbotron")
        h1='Vista de Productos'
        br
        if prods
            div(class="table-responsive")
                table(class="table table-dark")
                    tr
                        th='Nombre'
                        th='Precio'
                        th='Foto'
                        each producto in prods
                            tr
                                td=producto.producto
                                td=producto.precio
                                td  
                                    img(width="50" src=producto.imag alt="not found")
        else
            h3(class="alert alert-warning")='No se encontraron productos'
        br
        a(href="/" type="button" class="btn btn-primary")='Cargar un nuevo producto'